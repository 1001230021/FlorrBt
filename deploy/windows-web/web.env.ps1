$env:WEB_HOST = "0.0.0.0"
$env:WEB_PORT = "8080"

# If the game server runs on the same Windows server, keep 127.0.0.1.
# Otherwise change this to the game server IP or domain.
$env:GAME_HOST = "127.0.0.1"
$env:GAME_PORT = "10012"
